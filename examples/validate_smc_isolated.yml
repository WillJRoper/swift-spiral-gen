extends: mw_m31_merger.yml
select_galaxies: [SMC]
output:
  ics: validate_smc_isolated.hdf5
  params: validate_smc_isolated.yml
  run_name: validate_smc_isolated
  snapshot_basename: validate_smc_snapshot
orbit:
  type: manual
simulation:
  box_kpc: 250
  time_end_gyr: 2.0
  snapshot_dt_myr: 50
grid:
  max_top_level_cells: 4
